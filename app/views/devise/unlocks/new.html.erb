<h2 class="ui teal header">
  <div class="content">
    <%= t('devise.unlocks.resend_unlock_instructions') %>
  </div>
</h2>

<%= form_for(resource, as: resource_name,
                       url: unlock_path(resource_name),
                       html: { method: :post, class: "ui large error form" }) do |f| %>

  <div class="ui piled segment">
    <div class="field">
     <div class="ui left icon input">
        <i class="mail icon"></i>
        <%= f.email_field :email, autofocus: true, placeholder: t('devise.sessions.email') %>
      </div>
    </div>

    <div class="actions">
      <%= f.submit I18n.t('devise.unlocks.resend_unlock_instructions'),
                  class: "ui fluid large teal submit button" %>
    </div>
  </div>

  <%= devise_error_messages! %>
<% end %>

<div class="ui message">
  <%= render "devise/shared/links" %>
</div>
